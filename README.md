shell.vim
=========

Runs shell commands from VIM and puts them in a scratch window

Usage:
------

Examples:

```vim
:Shell ls -la
```

```vim
:Shell git status
```

To run the same command again:

```vim
:Shell!
```

